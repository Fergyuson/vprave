services:
  frontend-builder:
    container_name: vprave-frontend-builder
    build:
      context: ./frontend
      dockerfile: Dockerfile
    volumes:
      - ./frontend/dist:/app/static
    command: sh -c "cp -r /static/* /app/static/ && echo 'Frontend built and copied'"

  app:
    container_name: vprave-app
    build:
      context: ./backend
      dockerfile: Dockerfile
    volumes:
      - ./backend/logs:/app/logs
      - ./frontend/dist:/app/static
    environment:
      - BACKEND_PORT=4014
    ports:
      - "127.0.0.1:4014:4014"
    restart: unless-stopped
    networks:
      - vprave-net

networks:
  vprave-net:
    driver: bridge
